"""Handles for Web resources."""

class SubjectHandle(object):
    def __init__(self, identifier, name, properties, links, cache_id):
        self.url = url
        self.name = name
        self.properties = properties
        self.data_dir = data_dir
        self.tarfile = tarfile
